;; Invariants for `last`.

(last [10 20 30]) -- 30
(last [42]) -- 42

;; last(v) == at(v, count-1)
(set V (rand 256 10000))
(last V) -- (at V (- (count V) 1))

;; last(reverse v) == first v
(first V) -- (last (reverse V))
